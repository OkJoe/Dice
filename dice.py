#! /usr/bin/env python
import random
import intInput
s=0
dices=intInput.int_input("How many dices would you like to have")
while s<dices:
    s+=1
    while True
        sides=intInput.int_input("How many sides would you like to have")
        if sides>1:
            break
    print (random.randint(1, sides))